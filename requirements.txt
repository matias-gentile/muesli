anthropic>=0.111.0
faster-whisper>=1.0.0
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24
flask>=3.0
python-dotenv>=1.0
notion-client>=2.2.1

# Solo para la app de barra de menú de macOS (python menubar.py):
rumps>=0.4.0 ; sys_platform == "darwin"
pywebview>=5.0 ; sys_platform == "darwin"   # opcional: panel.py standalone
pyobjc-framework-AVFoundation>=9.0 ; sys_platform == "darwin"   # pedir permiso de micrófono
pyobjc-framework-WebKit>=9.0 ; sys_platform == "darwin"   # ventana nativa del panel (WKWebView)
